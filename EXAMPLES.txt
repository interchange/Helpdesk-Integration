set -e

# the following examples have all the --debug flag set, which prevents
#  the removing of the mails from the inbox

# list all the mails from marco and exit
./bin/copy-mail-to-rt.pl --debug --from marco --dry-run

# copy the mails from marco in a new ticket under General
./bin/copy-mail-to-rt.pl --debug --from marco --queue "General"

# copy the mails from marco in the TW task list "New thing" (and
# create it if needed)
./bin/copy-mail-to-rt.pl --debug --from marco --target teamwork --queue "New thing"

# copy the mails from marco in the RT ticket 2
./bin/copy-mail-to-rt.pl --debug --from marco --ticket 2

# copy the  mails from marco in the TW task 1159403 as comments
./bin/copy-mail-to-rt.pl --debug --from marco --target teamwork --ticket 1159403

# copy the mails from marco in the ticket 2 as comment
./bin/copy-mail-to-rt.pl --debug --from marco --ticket 2 --comment

# copy the mails in a new RT ticket setting the subject
./bin/copy-mail-to-rt.pl --debug --from marco --bts-subject "Testing the subj"

# same for teamwork
./bin/copy-mail-to-rt.pl --debug --from marco --bts-subject "Testing the subj" --queue Test --target teamwork

# same for teamwork, but assigning to workers
./bin/copy-mail-to-rt.pl --debug --from marco --bts-subject "Testing the subj" --queue Test --target teamwork --workers "melmothx@gmail.com,bubez"


# copy the RT ticket 1 in the TW task "This is a test" (and create it
# if needed).
./bin/copy-mail-to-rt.pl --source rt --target teamwork --bts-ticket 150 --queue "Pallino"

# sameb thing, but assign to a username

./bin/copy-mail-to-rt.pl --source rt --target teamwork --bts-ticket 151 --queue "This is a test" --workers "melmothx@gmail.com,bubez"

# copy the RT ticket 1 in the TW task 1159613 as comments
./bin/copy-mail-to-rt.pl --source rt  --target teamwork --bts-ticket 151 --queue "This is a test" --workers "melmothx@gmail.com,bubez" --ticket 1159613


# custom fields

# Assuming RT#155 has the custom fields "Remote system" set to
# "teamwork" and the "Remote queue" set to "Hello", with the following
# configuration:
# 

# rt:
#   type: rt
#   server: http://localhost/rt
#   user: root
#   password: xxxx
#   target_name_field: "Remote system"
#   target_id_field: "Teamwork id"
#   target_queue_field: "Remote queue"
# teamwork:
#   type: teamwork
#   password: xxxxxxx
#   server: https://linuxiahr.teamworkpm.net
#   project: Linuxia testing
# 

# the following command will discovery the target, and create a new
#  todo-list in the Task list "Hello"

bin/copy-mail-to-rt.pl --source rt --bts-ticket 155 
